name: Custom Environment Vriables

on: push

env:
  SUBSCRIBE: Prod Region

jobs:
  TruX:
    runs-on: ubuntu-latest
    env:
      DEV: Dev Region
    steps:
      - name: "Trux DevOps."
        run: echo "$DEV , $SUBSCRIBE and $MO channel."

        env:
          MO: Mo Region